
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:toolbarStyle">@style/AppTheme.AppBarOverlay</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:screenOrientation">portrait</item>
    </style>

    <style name="FullScreenTheme" parent="AppTheme.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="WelcomeTheme" parent="FullScreenTheme">
        <item name="android:background">@drawable/bg</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="common_btn_style_base">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/button_bg</item>
    </style>

    <style name="common_btn_style" parent="common_btn_style_base">
        <item name="android:layout_width">63dp</item>
        <item name="android:layout_height">35dp</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="common_btn_style_green" parent="common_btn_style">
        <item name="android:background">@drawable/button_bg_green</item>
    </style>

    <style name="common_btn_style_big" parent="@style/common_btn_style_base">
        <item name="android:layout_width">81dp</item>
        <item name="android:layout_height">45dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="anim_add_comment">
        <item name="android:windowEnterAnimation">@anim/expand_right_enter</item>
        <item name="android:windowExitAnimation">@anim/expand_right_exit</item>
    </style>
</resources>
